%% Example application: an agent loop over erllama:chat/3.
%% Run from this directory: rebar3 shell, then
%%   agent_loop:run("/path/to/model.gguf", <<"What time is it, and what is 17 * 23?">>).
{erl_opts, [debug_info]}.

{deps, [
    {erllama, "~> 0.10.0"}
]}.

{shell, [{apps, [agent_loop]}]}.
